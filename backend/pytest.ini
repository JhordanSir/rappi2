[pytest]
testpaths = tests
# Tests async sin decorar uno a uno; un único event loop por sesión para que el pool
# de conexiones asyncpg (creado a nivel de módulo) no cruce loops distintos.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
