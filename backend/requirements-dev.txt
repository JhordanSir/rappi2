# Dependencias de desarrollo/pruebas (no van en la imagen de producción).
# Instalar y correr DENTRO del contenedor api:
#   docker compose exec api pip install -r requirements-dev.txt
#   docker compose exec api pytest
-r requirements.txt
pytest
pytest-asyncio
